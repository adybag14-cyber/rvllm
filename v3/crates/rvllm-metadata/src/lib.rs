// rvllm-metadata — scaffold only. See v3/specs/08-metadata.md.
//   pub mod layout;  // MetadataLayout keyed by (bucket, max_blocks)
//   pub mod pack;    // MetaPack::upload(layout, batch, scope) -> MetaHandle
//   pub mod handle;  // MetaHandle: opaque borrow; offsets fixed at init
