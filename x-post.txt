updated rvLLM and synced the public repo/docs/paper/site to the current clean H100 results.

current direct-engine comparison against vLLM 0.19.0 on the same H100, same Qwen2.5-7B snapshot, same output len:

- N=1: 132.7 tok/s vs 167.5
- N=32: 4494.9 tok/s vs 4964.2
- N=64: 8503.4 tok/s vs 9312.6
- N=96: 10530.6 tok/s vs 13085.9
- N=128: 13718.1 tok/s vs 16825.3

so the honest headline is:

- current clean main is still behind vLLM across these direct-engine buckets
- the old `89f` `N=64` win was real as a measurement, but invalid as a benchmark claim
- that fast path skipped the FFN down-projection inside the CUTLASS gate-aux branch

the important architecture corrections were:

- the normal T=1 path is the reusable batched scratch path, not the old `CublasGemvDecode` story
- the batched GEMM policy is explicit instead of half-wired
- the real N=64 hotspot was Hopper FFN `gateup_silu -> down`
- the recovered fast H100 CUTLASS `.so` is now archived in the repo for forensic reproducibility only

the batched side is still the same explicit hybrid policy:

- QKV / O / down on cuBLAS or cublasLt
- gate activation on CUTLASS SM90 aux epilogue

tooling mattered too:

- stable direct benchmark harness
- fixed `cublasLt` autotune-cache replay
- runner-side phase timing on the real decode bucket
- CUTLASS shared-library + Rust FFI loop for real H100 epilogue work
- artifact recovery from the original H100 so the invalid fast path could be explained instead of hand-waved

still true: stock vLLM has the stronger decode stack overall on the current clean reproducible build.

repo: https://github.com/m0at/rvllm
site: https://m0at.github.io/rvllm/
paper: https://m0at.github.io/rvllm/paper/rvllm.pdf
